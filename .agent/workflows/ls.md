---
description: List files and directories
---

<!-- ANTIGRAVITY/GEMINI-CLI DIRECTIVE: `// turbo-all` is an
     Antigravity-specific instruction that fans this workflow
     out to ALL active agents in parallel. It is NOT standard
     markdown and will be ignored by other tools (Claude Code,
     Codex, Cursor, etc.). Remove or comment it out when using
     this workflow outside of Antigravity/Gemini CLI. -->
// turbo-all

1. List contents: `ls -F`
