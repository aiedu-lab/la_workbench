# Solution: <Exercise Title - example: Gaussian Elimination>

## Contributors
<One bare GitHub-UserId per line - example:
adisarcar
sidk256>

## Summary
<Solution Summary - example:
For solving a multi-variable system of linear equations,
we used Gaussian elimination via code.>

## Solution Manual
<Steps to run the solution - example:
* Run gaussian elimination:
  `python3 gaussian_elimination.py`.
* Confirm the printed solution satisfies six equations:
  `x1, x2, x3, x4, x5, x6`>

## Test Cases
<Example test cases ran to validate the solution - example:
Constructed two examples of systems of equations with known
solutions for backtesting.>

## Software Installs
<Environment setup and installs - example:
* NO installs required beyond the repo's `.venv` that already
has numpy — see `requirements.in`.
* For local .venv setup and install, run:
```bash
  python3 -m venv .venv &&\
  source .venv/bin/activate &&\
  pip install -r requirements.in
```
>
